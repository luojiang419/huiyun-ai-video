# 140_自更新方案完整文档沉淀与复用指南

## 功能描述和目标

- 将当前项目已经落地的 Windows 桌面软件自更新方案完整沉淀为一份可复用于其他软件的文档。
- 覆盖客户端、独立更新器、安装器、发布脚本、后端接口、状态文件、发布策略和迁移改造点。
- 明确当前项目的真实可用路径是“完整安装包更新”和“补丁安装包更新”，并给出推荐复用顺序。

## 涉及的文件路径清单（精确到行号）

- `doc/Windows桌面软件自更新方案复用指南.md:1`
- `pond5_clip_manager/lib/main.dart:10`
- `pond5_clip_manager/lib/app.dart:36`
- `pond5_clip_manager/lib/core/constants.dart:44`
- `pond5_clip_manager/lib/core/app_paths.dart:77`
- `pond5_clip_manager/lib/models/app_update_info.dart:52`
- `pond5_clip_manager/lib/models/app_update_state.dart:4`
- `pond5_clip_manager/lib/services/app_update_service.dart:302`
- `pond5_clip_manager/lib/providers/app_update_provider.dart:12`
- `pond5_clip_manager/lib/widgets/app_update_gate.dart:11`
- `pond5_clip_manager/lib/pages/app_updater_page.dart:9`
- `tools/build_update_bundle.ps1:1`
- `tools/build_installer.ps1:1`
- `tools/build_patch_installer.ps1:1`
- `tools/publish_update_release.ps1:1`
- `tools/installer/Pond5ClipManager.iss:1`
- `tools/pond5_lunch_update_backend/app/main.py:37`
- `tools/pond5_lunch_update_backend/app/service.py:449`
- `tools/pond5_lunch_update_backend/tests/test_service.py:81`

## 关键代码片段（包含改动前/后的对比）

```md
// after
# Windows桌面软件自更新方案复用指南
```

```dart
// current client check entry
final uri = Uri.parse(
  '$normalizedBaseUrl/api/pond5-update/v1/check/windows/x86_64/$currentVersion',
);
```

```dart
// current installer execution
final process = await Process.start(waitingJob.packagePath, [
  '/VERYSILENT',
  '/RESULTFILE=${waitingJob.resultFilePath}',
  '/STARTUPCONFIRMTOKEN=${waitingJob.startupConfirmToken}',
]);
```

```python
if patch_meta and str(patch_meta.get("baseVersion", "")).strip() == current_version:
    candidate = release_patch_installer_path(settings, latest_version)
    if candidate.exists():
        package_url = patch_installer_url(settings, latest_version)
        installer_mode = "patch"
```

## 引用的类名、方法名、参数名

- `AppUpdateService.confirmStartupIfNeeded`
- `AppUpdateService.checkForUpdate`
- `AppUpdateService.downloadUpdate`
- `AppUpdateService.launchUpdater`
- `AppUpdateService.runUpdaterSession`
- `AppUpdateNotifier.initialize`
- `AppUpdateNotifier.applyPendingUpdate`
- `AppUpdateGate`
- `AppUpdaterPage`
- `build_check_payload()`
- `check_for_update()`
- `publish_release()`
- `patchInstallerExe`
- `installerMode`
- `startupConfirmToken`

## 数据模型变更（字段增删改）

- 无代码数据模型新增或修改
- 本轮主要是文档沉淀，归纳了现有更新模型字段：
  - `AppUpdateInfo`
  - `PendingAppUpdateJob`
  - `AppUpdateResult`
  - 服务端 `releases` 发布记录

## 依赖关系和调用链

1. 主程序启动：
   - `main.dart -> AppPaths.ensureDirectories()`
   - `main.dart -> AppUpdateService.confirmStartupIfNeeded()`
2. 主界面更新监听：
   - `app.dart -> AppUpdateGate -> appUpdateProvider.initialize()`
3. 自动检查与下载：
   - `AppUpdateNotifier -> AppUpdateService.checkForUpdate()`
   - `AppUpdateNotifier -> AppUpdateService.downloadUpdate()`
4. 独立更新器安装：
   - `AppUpdateNotifier.applyPendingUpdate()`
   - `AppUpdateService.launchUpdater()`
   - `AppUpdaterPage -> AppUpdateService.runUpdaterSession()`
5. 安装器与 helper：
   - `Pond5ClipManager.iss`
   - `RESULTFILE`
   - `STARTUPCONFIRMTOKEN`
6. 服务端：
   - `publish_update_release.ps1 -> POST /releases/publish`
   - `check_for_update() -> build_check_payload()`

## 测试验证方法

- 本轮为文档沉淀，没有新增运行时代码。
- 已人工对照以下实现，确认文档描述与当前项目实际链路一致：
  - `pond5_clip_manager/lib/services/app_update_service.dart`
  - `pond5_clip_manager/lib/providers/app_update_provider.dart`
  - `pond5_clip_manager/lib/widgets/app_update_gate.dart`
  - `pond5_clip_manager/lib/pages/app_updater_page.dart`
  - `tools/build_update_bundle.ps1`
  - `tools/build_installer.ps1`
  - `tools/build_patch_installer.ps1`
  - `tools/publish_update_release.ps1`
  - `tools/installer/Pond5ClipManager.iss`
  - `tools/pond5_lunch_update_backend/app/main.py`
  - `tools/pond5_lunch_update_backend/app/service.py`
  - `tools/pond5_lunch_update_backend/tests/test_service.py`

## 开发完成状态

- 已完成
