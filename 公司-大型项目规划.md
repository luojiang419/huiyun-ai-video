我现在要处理一个大型、多轮、长上下文任务，请你严格遵守以下规则：
全程使用中文和我交流

1. 任务模式
- 先做整体规划，不直接开始写完整内容。
- 输出清晰的：任务目标、拆分步骤、文件/模块清单、待办清单、验收标准。
- 每次完成后告诉我当前进度，不要一次性输出超长内容。

2. 上下文与记忆规则
  - 每完成一次功能的修改就自动生成一份「进度快照.md」到"\进度快照"文件夹，包含：
  - 标题名称里包含该次简要的功能修改描述，标题前面要有数字序号标识，如果之前的没有，则在开始修改前，将所有进度快照都重新按时间顺序补充序号，方便查阅。
  - 已完成内容
  - 当前修改到哪一行/哪一个模块
  - 待办清单（未完成）
  - 下一步要做什么
  - 当上下文快满时，主动总结，不要让任务中断。

3. 续写规则
- 记住我们约定的：格式、风格、结构、命名规范、技术栈。
- 不要重复已完成的内容，只从**上一次结束的位置继续**。
- 修改时只输出改动部分或完整新版，提前说明是哪种。

4. 中断恢复规则
- 如果我新开对话、刷新页面、更换设备，我会给你上一轮的「进度快照」。
- 你接到后，直接从上一轮结束的位置继续，不再重复提问确认

5.操作开始前，先读取之前的进度快照，如果任务里提到之前快照出现的地方，从上次记录的结束位置分析，

7.备份规则:
用户提出备份需求时，自动将当前版本的源代码备份到“G:\data\app\AI\backup”文件夹里，自动分析backup文件夹里已经存在的备份文件，然后从最近的一个备份版本号自动递增版本号备份

8.遇到网络问题时，可以使用代理网络地址“获取本机ip:7890”

9.flutter在G:\data\flutter文件夹

10.命令里我所指的
影视版就是“G:\data\app\AI\flutter_grsai_video_gen”项目


11.首先检查项目编译和开发需要什么环境，先将依赖安装完毕，如果遇到卡顿自动解决

6.编译和命名规则：
用户提示编译新版本时
- Windows版本后续默认编译为安装包，不再编译便携版
- 编译到"G:\data\app\AI\dist\影视版\影视版-V版本号"
- 编译输出路径规则：
  * 影视版：dist/影视版/影视版-V版本号

- 命名规则："影视版-V版本号"（例如：影视版-V1.0）
- Windows安装包文件命名规则："影视版-安装包-V版本号.exe"（例如：影视版-安装包-V1.0.exe）
- Windows安装包升级规则：安装器必须使用固定 AppId，并优先识别系统中已安装版本的真实安装目录；如果用户之前自定义安装路径，新安装包默认定位到原目录执行覆盖升级，不重新安装到新的默认目录
- Windows安装包默认安装目录规则：
  * 首次安装默认目录改为 `D:\Program Files\VideoGen`
  * 如果系统中已存在旧版本安装目录，优先沿用旧目录，不强制改到新默认目录
  * 如果目标机器不存在 `D:` 盘，则允许自动回退到系统 Program Files 目录
  * 由于默认目录位于 Program Files，安装器必须使用管理员权限执行安装
- Windows安装包无感升级保留用户数据规则：
  * 升级安装时严禁覆盖用户已有的"{app}\data\Settings\config.json"和"{app}\data\Settings\system_prompt.txt"，避免清空用户API配置、模型配置、视频节点、生成参数和自定义提示词。
  * 安装器[Files]主递归规则必须排除用户配置文件，例如：
    Source: "..\build\windows\x64\runner\Release\*"; DestDir: "{app}"; Excludes: "data\Settings\config.json,data\Settings\system_prompt.txt"; Flags: ignoreversion recursesubdirs createallsubdirs
  * 默认配置文件只能作为首次安装兜底写入，必须单独使用"onlyifdoesntexist uninsneveruninstall"，例如：
    Source: "..\build\windows\x64\runner\Release\data\Settings\config.json"; DestDir: "{app}\data\Settings"; Flags: ignoreversion onlyifdoesntexist uninsneveruninstall
    Source: "..\build\windows\x64\runner\Release\data\Settings\system_prompt.txt"; DestDir: "{app}\data\Settings"; Flags: ignoreversion onlyifdoesntexist uninsneveruninstall
  * 每次编译安装包前必须检查windows/installer.iss是否仍保留上述Excludes和onlyifdoesntexist规则；如果缺失，先修复再编译。
  * 生成安装包后必须在进度快照中记录：安装器固定AppId、真实安装目录识别逻辑、配置文件不会覆盖用户已有数据。
- 配置文件安全规则（永久执行，防止再次出现“升级后重新配置”）：
  * `data\Defaults\config.json` 只能作为运行时读取兜底，禁止通过 `initConfigFiles()`、`saveApiConfigs()`、`saveGenerateParams()`、`saveVideoSettingsConfig()`、`saveVideoNodes()`、`saveVideoGallery()` 等保存链路写回 `data\Settings\config.json`。
  * 程序启动时如果 `data\Settings\config.json` 存在且内容有效，严禁重写、覆盖或用默认配置替换。
  * 所有写入 `data\Settings\config.json` 的链路必须统一走单一配置保存服务，禁止在业务层直接 `writeAsString()`、禁止多处并发直接覆盖同一个配置文件。
  * 配置文件保存必须使用“临时文件写入 -> flush -> 原子替换正式文件”的安全写法，严禁直接原地覆盖 `config.json`，避免升级关闭程序或异常退出时把文件写坏。
  * 每次对 `data\Settings\config.json` 发生有效改写前，必须自动生成 `config.json.bak-时间戳` 本地备份；备份允许按数量裁剪，但必须至少保留最近若干份可恢复版本，禁止只保留当前文件不留历史。
  * 程序启动时如果 `data\Settings\config.json` 缺失，必须先按顺序尝试恢复：
    1. 当前安装目录中按时间倒序检查可解析的 `data\Settings\config.json.corrupt-*`
    2. 当前安装目录中按时间倒序检查 `data\Settings\config.json.bak-*`
    3. 历史安装目录中的 `data\Settings\config.json`
    4. 历史安装目录中按时间倒序检查 `data\Settings\config.json.corrupt-*`
    5. 历史安装目录中按时间倒序检查 `data\Settings\config.json.bak-*`
    6. 只有完全找不到可恢复配置时，才允许创建首装默认配置
  * 程序启动时如果 `data\Settings\config.json` 损坏或 JSON 解析失败，必须先备份为 `config.json.corrupt-时间戳`，再进入恢复流程，严禁静默直接覆盖。
  * 恢复链路选择候选配置时，必须按“可解析且最后修改时间更新优先”的原则恢复，禁止优先捡回更旧的空备份，导致用户真实配置被较旧默认/空配置顶掉。
  * 如果用户之前把软件装在旧目录，新目录首次启动时必须优先识别并迁移旧目录配置，避免因为安装路径变化导致重新配置。
  * 如果安装器使用 `CloseApplications=yes` 或升级过程会主动关闭程序，必须额外确保配置保存链路具备串行化和安全落盘能力，不能假设升级时不会打断写配置。
  * 每次发布或编译新安装包前，必须验证以下场景至少不回归：
    1. 升级安装后用户已有 `data\Settings\config.json` 不被覆盖
    2. `config.json` 缺失但存在 `config.json.bak-*` 时可自动恢复
    3. `config.json` 损坏时会先生成 `config.json.corrupt-*` 再恢复
    4. 旧安装目录存在配置时，新目录首次启动可自动迁移
    5. 当前安装目录同时存在较新的 `config.json.corrupt-*` 和较旧空 `config.json.bak-*` 时，会优先恢复较新的有效配置
    6. 升级关闭程序或高频保存参数后，`config.json` 不会因写入中断而变成空文件、半截 JSON 或被旧空备份覆盖
  * 每次完成与配置初始化、安装器、默认配置、保存逻辑相关的修改后，必须补充对应测试或复用已有测试，重点覆盖“默认配置不落盘”“缺失恢复”“损坏恢复”“旧安装目录迁移恢复”。
  * 上述测试还必须补充或复用覆盖“安全写入原子替换”“连续保存自动生成本地备份”“较新 `.corrupt-*` 优先恢复”的场景，避免只测缺失恢复、不测真实升级写坏后的恢复。
  * 已知用户当前安装目录时，发布前或修复后必须优先针对该真实目录做一次现场核对；本项目当前影视版默认重点验收目录为 `D:\Program Files\VideoGen`。
  * 每次生成安装包后，必须在进度快照中额外记录：本次是否检查了上述配置安全规则、恢复链路和验证结果。
- 版本号从V1.0开始递增，自动分析已存在的最新版本号并递增
- 编译新版本时，软件内标题旁边显示的版本号也要同步更新为当前编译版本号
- 编译新版本时不删除旧版本文件夹
- backup文件夹备份命名规则相同："影视版-V版本号"


现在请你：
1）认真理解这套规则；
2）告诉我你已准备好；
3）我再把具体任务发给你。

---
name: karpathy-guidelines
description: Behavioral guidelines to reduce common LLM coding mistakes. Use when writing, reviewing, or refactoring code to avoid overcomplication, make surgical changes, surface assumptions, and define verifiable success criteria.
license: MIT
---

# Karpathy Guidelines

Behavioral guidelines to reduce common LLM coding mistakes, derived from [Andrej Karpathy's observations](https://x.com/karpathy/status/2015883857489522876) on LLM coding pitfalls.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.
